pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build and Test with Cache') {
            steps {
                dir('Chapter08/sample1') {
                    sh 'bash gradlew test'
                }pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Unit Test') {
            steps {
                dir('Chapter08/sample1') {
                    sh 'bash gradlew test'
                }
            }
        }
        stage('Code Coverage') {
            when {
                branch 'master'
            }
            steps {
                dir('Chapter08/sample1') {
                    sh 'bash gradlew jacocoTestReport'
                    sh 'bash gradlew jacocoTestCoverageVerification'
                }
            }
        }
        stage('Feature Branch Tests') {
            when {
                expression { env.BRANCH_NAME ==~ /.*feature.*/ }
            }
            steps {
                echo "Feature branch detected: ${env.BRANCH_NAME}"
                dir('Chapter08/sample1') {
                    sh 'bash gradlew test'
                }
            }
        }
        stage('Validate Branch') {
            when {
                expression {
                    !(env.BRANCH_NAME == 'master' || env.BRANCH_NAME ==~ /.*feature.*/)
                }
            }
            steps {
                error("Branch '${env.BRANCH_NAME}' is not allowed. Only 'master' and feature branches are permitted.")
            }
        }
    }
}
            }
        }
    }
}